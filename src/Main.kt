fun main() {
    /*val soma = Soma()

    // Entrada de dados usando InputUtils
    soma.A = InputUtils.lerNumero("Digite o valor de A:")
    soma.B = InputUtils.lerNumero("Digite o valor de B:")
    soma.C = InputUtils.lerNumero("Digite o valor de C:")

    // Processamento e Saída de dados
    soma.calcularSoma()
    val resultado = soma.resultado
    println("A soma de A e B é: $resultado")

    if (soma.isSomaMenorQueC()) {
        println("A soma é menor que C.")
    } else {
        println("A soma não é menor que C.")
    }*/

   /* // NumeroUtils - par ou impar
    val numero = InputUtils.lerNumero("Digite um número para verificar se é par ou impar, positivo ou negativo:")
    println("O número $numero é ${NumeroUtils.verificarParOuImpar(numero)}.")
    println("O número $numero é ${NumeroUtils.verificarPositivoOuNegativo(numero)}.")*/

   /* val calculo = Calculo()

    // Realiza o cálculo
    calculo.realizarCalculo()

    // Exibe o resultado
    calculo.exibirResultado()*/

/*    // Recebe um número do usuário
    val numero = InputUtils.lerNumero("Digite um número inteiro:")

    // Cria uma instância de AntecessorSucessor e exibe os resultados
    val antecessorSucessor = AntecessorSucessor(numero)
    antecessorSucessor.exibirAntecessorESucessor()*/

    /*val salarioUsuario = InputUtils.lerDecimal("Digite o valor do salário do usuário:")
    val salario = Salario(salarioUsuario)
    salario.exibirResultado()*/

  /*  // Recebendo o valor do usuário
    val valor = InputUtils.lerDecimal("Digite um valor para reajustar:")

    // Criando uma instância da classe Reajuste e exibindo o resultado
    val reajuste = Reajuste(valor)
    reajuste.exibirResultado()*/

/*// Recebendo os valores booleanos do usuário
    val valor1 = InputUtils.lerBooleano("Digite o primeiro valor booleano (true/false):")
    val valor2 = InputUtils.lerBooleano("Digite o segundo valor booleano (true/false):")

    // Criando uma instância da classe VerificacaoBooleanos e exibindo o resultado
    val verificacao = VerificacaoBooleanos(valor1, valor2)
    verificacao.exibirResultado()*/

    /*val ordenacao = Ordenacao()

    // Adiciona números à lista
    println("Digite três valores inteiros:")
    for (i in 1..3) {
        val numero = InputUtils.lerNumero("Digite o valor $i:")
        ordenacao.adicionarNumero(numero)
    }

    // Imprime a lista ordenada em ordem decrescente
    ordenacao.imprimirOrdenadoDecrescente()*/

   /* // Entrada de dados
    val peso = InputUtils.lerDecimal("Digite o peso (em kg):")

    val altura = InputUtils.lerDecimal("Digite a altura (em metros):")

    // Cria uma instância da classe IMC
    val imc = IMC(peso, altura)

    // Exibe a condição do IMC
    println(imc.condicaoIMC())*/

    // Entrada de dados
    val nota1 = InputUtils.lerDecimal("Digite a primeira nota:")

    val nota2 = InputUtils.lerDecimal("Digite a segunda nota:")

    val nota3 = InputUtils.lerDecimal("Digite a terceira nota:")

    // Cria uma instância da classe Notas
    val notas = Notas(nota1, nota2, nota3)

    // Imprime a média das notas
    notas.imprimirMedia()





}